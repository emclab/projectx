<div id="main_body">
  <h4><%= t(@title) %></h4>
  <%= simple_form_for @model,  :method => :put, :url =>  eval(@search_stat.search_results_url)  do |f| %>
      <%= render inline: ERB.new(@erb_code).result(binding) %>
  <% end %>
  <%= link_to t('Back'), SUBURI + '/authentify/view_handler?index=0' %>

</div>
