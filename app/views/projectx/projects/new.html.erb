<div id="main_body">
<h4><%= t(@title) %></h4>

  <%= render 'form_new', :locals => { :project => @project} %>

<%= link_to 'Back', SUBURI + "/authentify/view_handler?index=0", :class => BUTTONS_CLS['action'] %>
</div>