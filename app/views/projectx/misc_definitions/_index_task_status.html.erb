
  <h4>Task Status</h4>
  <table class="table table-striped">
    <tr>
      <th>#</th>
      <th><%= t('Create Date</th>
      <th><%= t('TaskStatus</th>
      <th><%= t('Brief Note</th>
      <th><%= t('Ranking Order</th>
      <th><%= t('Entered By</th>
      <th></th>
    </tr>

    <% @misc_definitions.each do |qs| %>
        <tr>
          <td><%= qs.id %></td>
          <td><%= (qs.created_at + 8.hours).strftime("%Y/%m/%d")%></td>
          <td><%= qs.name %></td>
          <td><%= qs.brief_note %></td>
          <td><%= qs.ranking_order %></td>
          <td><%= qs.last_updated_by.name %></td>
          <td><%= link_to t('Edit'), edit_misc_definition_path(qs, :subaction => 'task_status', :format => :js), 
                  :remote => true, :class =>'btn btn-primary' if has_action_right?('update_task_status', 'projectx_misc_definitions', qs) %></td>
        </tr>
    <% end %>

  </table>

	<table class="table table-striped">
	  <tr>
	    <th>
	    	&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
			<%= link_to t('New Project Status'), new_misc_definition_path(:for_which => 'project_status', :subaction => 'project_status', :format => :js), :remote => true, 
               :id => 'new_log_link', :class =>'btn btn-primary' if has_action_right?('create_project_status', 'projectx_misc_definitions') %>   
		</th>
	  </tr>
	</table>
  <%= link_to t('Back'), SUBURI + "/authentify/view_handler?index=0", :id => 'close' %>
