
  <h4>Task Status</h4>
  <table class="table table-striped">
    <tr>
      <th>#</th>
      <th>Create Date</th>
      <th>TaskStatus</th>
      <th>Brief Note</th>
      <th>Ranking Order</th>
      <th>Entered By</th>
      <th></th>
    </tr>

    <% @misc_definitions.each do |qs| %>
        <tr>
          <td><%= qs.id %></td>
          <td><%= (qs.created_at + 8.hours).strftime("%Y/%m/%d")%></td>
          <td><%= qs.name %></td>
          <td><%= qs.brief_note %></td>
          <td><%= qs.ranking_order %></td>
          <td><%= qs.last_updated_by.name %></td>
          <td><%= link_to 'Edit', edit_misc_definition_path(qs, :subaction => 'task_status', :format => :js), 
                  :remote => true if has_action_right?('update_task_status', 'projectx_misc_definitions', qs) %></td>
        </tr>
    <% end %>

  </table>

	<table class="table table-striped">
	  <tr>
	    <th>
	    	&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
			<%= link_to "New Project Status", new_misc_definition_path(:for_which => 'project_status', :subaction => 'project_status', :format => :js), :remote => true, 
               :id => 'new_log_link' if has_action_right?('create_project_status', 'projectx_misc_definitions') %>   
		</th>
	  </tr>
	</table>
  <%= link_to 'Back', SUBURI + "/authentify/view_handler?index=0", :id => 'close' %>
